@model IEnumerable<Flashcards.Models.Answer>

@{
    ViewBag.Title = "Statistic";
}

<h2>Statistic</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.FlashCard.Word1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FlashCard.Word2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CorrectAnswers)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.FlashCard.Word1)
    </td>

    <td>
        @Html.DisplayFor(modelItem => item.FlashCard.Word2)
    </td>

    <td>
        @Html.DisplayFor(modelItem => item.CorrectAnswers)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.UserID)
    </td>


   
</tr>
}

</table>
